# ivn-keyword-crosswalk
Identifies uncommon words in component descriptions that can serve as matching keys between a reference set (Components tab) and a new set (to-be-crosswalked tab). This helps in governance document management by finding semantic connections between components that may use similar specialized terminology.
